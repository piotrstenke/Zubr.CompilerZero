use System;
use System.Collections.Generic;
use System.Array as array;

// This is a comment

**

This is also a comment

**

int main()
{
	bool flag = true;

	if(flag)
	{
		//println("Hello");
	}
	elif(true)
	{
	}
	elif(false)
	{
	}
	else
	{
	}

	while(flag)
	{
		int a = 2;
		flag = false;
	}

	string b = "hello \tthere";

	do
	{
		char c = '5';

		bool d = (1 + -2.2) * 3 == (int)7 << 2 && foo(4) < 10;
	}
	while(!true);

	for (char a : b)
	{
	}

	println("Bajo jajo");

	return 1;
}

priv int foo(int a)
{
	return ++a;
}

pub void print(string message = "")
{
	Console.Write(message);
}

module top;

void println(mut string message)
{
	Console.WriteLine(message);
}

module Hello;

priv struct Test<T, U> where
	T : Clone, U,
	U : Clone, //class,
{
}

pub open class Clone
{
}

//pub trait Clone
//{
//	self clone();
//}

//give Clone to Program
//{
//	Program clone()
//	{
//		return self;
//	}
//}
